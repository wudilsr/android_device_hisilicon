安装方法：
1. 进入相应的arm-xxx-linux目录（“xxx”为编译器名称，比如hisiv200）。
   例如使用hisiv200编译器，则进入arm-hisiv200-linux目录。

2. 执行cross_install脚本：
   ./cross_install
   编译器会自动安装到服务器上。注意，安装需要root权限！
   如果安装成功，在PATH环境变量可以看到/opt/hisi-linux/x86-arm/arm-hisiv200-linux/target/bin:/
  


